const funcUpperCase = (str) => str.toUpperCase();
const funcFirstLetter = (str) => str.charAt(0);
const concatStrings = (str1, str2) => str1.concat(str2); 

module.exports = {
    funcUpperCase,
    funcFirstLetter,
    concatStrings,
};